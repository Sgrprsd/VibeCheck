# Dependencies
node_modules/
package-lock.json # Optional: keep if you want to lock versions, but strictly excluding node_modules is key. Actually, we usually keep package-lock.

# Environment Variables (CRITICAL)
.env
.env.local
.env.*.local

# Builds
dist/
build/

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS Files
.DS_Store
Thumbs.db

# IDEs
.vscode/
.idea/

# Backend specific
server/node_modules/
server/.env

# Frontend specific
client/node_modules/
client/dist/
